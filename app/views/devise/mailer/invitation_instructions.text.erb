<%= t("devise.mailer.invitation_instructions.hello", invited_by: @resource.invited_by.try(:full_name) || "Team Slyp") %>

<%= t("devise.mailer.invitation_instructions.intro") %>

<%= link_to t("devise.mailer.invitation_instructions.accept", invited_by: @resource.invited_by.try(:full_name) || "Someone"), accept_invitation_url(@resource, :invitation_token => @token) %>

<%= t("devise.mailer.invitation_instructions.bye").html_safe %>
<%= t("devise.mailer.invitation_instructions.from").html_safe %>

